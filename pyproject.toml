[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ipz_hid"
version = "0.1.0"
description = "HID utilities"
requires-python = ">=3.10"
dependencies = [
    "attrs>=23.0.0",
    "evdev>=1.6.0"
]

[tool.setuptools.packages.find]
where = ["src"]